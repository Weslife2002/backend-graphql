directive @upper on FIELD_DEFINITION

type User {
  _id: ID
  firstName: String
  lastName: String
  email: String
  username: String
  role: String @upper

  photo: String
  bio: String
  status: String

  createdAt: DateTime
  updatedAt: DateTime

  followerCount: Int
}
